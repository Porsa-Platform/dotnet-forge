Read dotnet-forge/constitution.prompt, then read every file it refers to recursively, and obey all of those instructions.
Read dotnet-forge/roles/refactorer.prompt, then read every file it refers to recursively, and follow all of those instructions.
